fn find_first() -> number {
    my i = 0;
    while (i < 100) {
        if (i * i > 50) {
            gives(i);
        }
        i = i + 1;
    }
    gives(0);
}

fn the_precious() {

    my result = find_first();
    say(result);
    gives(0);
}
